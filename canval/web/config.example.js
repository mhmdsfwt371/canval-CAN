// Copy to config.js and fill in from the Firebase console:
//   Project settings -> Your apps -> Web app -> SDK setup and configuration
//
// None of this is secret. Firebase web config identifies the project; it
// does not grant access. Access is decided by the service, which verifies
// the signed token against Google's published keys, and by the sign-in
// providers and authorised domains you allow in the console.
window.CANVAL_FIREBASE = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  appId: "1:000000000000:web:0000000000000000000000"
};
