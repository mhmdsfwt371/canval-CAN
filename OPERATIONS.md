# canval — دليل التشغيل

صفحة واحدة لأي حد غير محمد يحتاج يفهم النظام أو يصلّحه.
الموقع: https://mhmdsfwt371.github.io/canval-CAN/

## إيه اللي بيحصل كل ليلة

مهمة GitHub Actions اسمها **canval nightly** بتشتغل الساعة 1 بتوقيت UTC
(4 فجرًا بتوقيت السعودية):

1. بتسحب كتالوج ملفات الكان من XDM.
2. بتقرا إعدادات كل جهاز (اللي اتغير من آخر يومين بس — الباقي من الكاش).
3. بتسأل أفاقي برو عن عيّنة من كل ملف: مين بيبعت إشارات فعلًا.
4. بتصدّر `docs/data/*.json` وبترفعهم للموقع.

قاعدة البيانات نفسها (`canval.db`) عايشة في **Actions Cache** مش في
المستودع. لو الكاش اتمسح (بيحصل بعد أسبوع من غير تشغيل)، الليلة اللي
بعدها بتعيد البناء من الصفر — بتاخد نص ساعة بدل دقايق، مرة واحدة، وده
طبيعي مش عطل.

## لو المزامنة فشلت

- افتح تبويب **Actions** في المستودع وشوف آخر تشغيل أحمر — الرسالة
  بتقول السبب في آخر 30 سطر غالبًا.
- أشهر سببين:
  - **رمز البرو انتهى** → جدّده (الخطوات تحت).
  - **XDM غيّر حاجة** → الفحص بيطبع أول رد HTML بدل JSON؛ ده معناه
    المسار اتغير ومحتاج مراجعة كود.
- فشل ليلة واحدة مش مشكلة: الموقع بيفضل شغال بآخر بيانات ناجحة.

## تجديد رمز أفاقي برو (كل شهر — الحالي ينتهي 2026-09-03)

1. سجّل دخول على منصة أفاقي برو وخد JWT جديد.
2. افتح: `Settings → Secrets and variables → Actions` في المستودع
   (https://github.com/mhmdsfwt371/canval-CAN/settings/secrets/actions)
3. حدّث السر **AFAQY_TOKEN**.
4. شغّل **canval nightly** يدوي مرة من تبويب Actions للتأكيد.

⚠️ حط تذكير شهري في تقويمك يوم 1 — لو اتنسي، فحص الإشارات الحية بيقف
بصمت والباقي بيفضل شغال، فمحدش هيلاحظ غير متأخر.

## الأسرار

| السر | مصدره | ملاحظات |
|---|---|---|
| `XDM_CLIENT_ID` | ثابت: `external_afaqy_client` | مش سر فعليًا |
| `XDM_CLIENT_SECRET` | فريق XDM | **اتكشف في محادثات — يتبدّل** |
| `AFAQY_TOKEN` | تسجيل دخول البرو | JWT صلاحيته 30 يوم، **اتكشف — يتبدّل** |

القيم تتخزن في GitHub Secrets وكمتغيرات بيئة على جهاز محمد بس.
ممنوع تدخل أي ملف في المستودع.

## قرارات متعمدة (عشان محدش «يصلّحها»)

- **مفيش كاش أوفلاين في الصفحة** — البيانات ليلية والنت متوفر؛ الكاش
  هيعرض أرقام قديمة بس.
- **خانة النشاط «—»** لما المسح مبيجيبش بيانات نشاط: مجهول ≠ صفر.
- **hardware 16956 (LX42-EA) مستبعد** من المسح عمدًا.
- **المعرّفات ظاهرة** على الصفحة بقرار صريح؛ لو الجمهور اتوسع،
  أول خطوة حماية هي تسجيل دخول Firebase (الكود مجهّز له).

## سؤال مفتوح: فرق العدّ بين XDM والقاعدة

**الحالة: متسجّل ومش متحقَّق منه — 2026-08-10**

في أوتلاندر 2026، عمود `Used count` في XDM بيقول 25 جهاز موزعين على
خمس إعدادات، والقاعدة عندها 21 بس. الأربعة الناقصين **مش موجودين في
`device_can` أصلًا** (اتأكدنا: 21 من 21 عندهم `file_id`، يعني مش أجهزة
من غير ملف).

التفصيل:

| الإعداد | XDM | عندنا |
|---|---|---|
| `(XG3780)` | 15 | 13 |
| `XTCAN2G` | 8 | 7 |
| `(XG3780) Spark` | 1 | 0 |
| `(LX-45) Spark` | 1 | 1 |

الاحتمالات، ولسه محدش فرّق بينها:

1. **قراءة فشلت وقت المسح.** الكود بيرفض يسجّل الفشل كحقيقة، فالجهاز
   بيقع بره القاعدة. لو ده السبب، رن تاني ممكن يجيبهم.
2. **الإعداد متسند بس لسه ما اتطبّقش.** المسح بيقرا
   `currentConfigName`، فلو الجهاز لسه شايل اسم إعداد قديم بيتعدّ تحته.
   ساعتها الرقم بتاعنا هو الصح وعدّاد XDM هو المضلّل.
3. **أجهزة اتشالت والعدّاد قديم.**

⚠️ **المهم مش الأربعة دول** — بـ 21 أو 25 الإجابة للعميل واحدة. المهم
هو: **هل الفرق ده نمطي عبر كل العربيات؟** لو أيوه، يبقى فيه ~4% من
الأسطول بره الحسبة في كل مكان في الأداة، وده يغيّر أرقام كتير.

الفحص الحاسم موجود في `gap2.py` (بيسأل XDM عن كل الأجهزة ويقارن
بالقاعدة جهاز جهاز). محتاج يتشغل على جهاز فيه المتغيرات.

## الاختبارات

`node tests/ui.test.mjs` بيفحص سلوك الصفحة كله، وبيشتغل تلقائي مع كل
رفع (workflow اسمه tests). علامة حمرا = سلوك حد معتمد عليه اتغيّر.

## الملكية

المستودع حاليًا على حساب شخصي. لو الأداة بقت أصل للشركة:
`Settings → General → Transfer ownership` لمؤسسة الشركة، وبعدها
تتنقل الأسرار يدوي (مش بتتنقل مع المستودع).

## إضافة لوجو لماركة جديدة

اللوجوهات ملفات ثابتة في `docs/logos/`. أي ماركة ملهاش ملف بياخد دايرة
ملوّنة بأول حرف تلقائيًا — يعني مفيش حاجة بتتكسر لو ماركة جديدة ظهرت.
عشان تديها لوجو حقيقي: حط صورة PNG صغيرة (٩٦ بكسل) باسم الماركة
بحروف صغيرة والمسافات شرطات — `KING LONG` → `king-long.png` — وارفع.
